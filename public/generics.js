"use strict";
// reusable blocks of code
var addId = function (id) { return ({ id: id, name: 'me' }); };
addId({}); // whatever type we specify when we call it
addId('');
var inblack67 = {
    name: 'Aman',
    id: '1' || 2
};
